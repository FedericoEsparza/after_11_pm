describe GeneralTrigSolver do
  describe '#solvable_factor?' do
    # it '' do
    #   solver = GeneralTrigSolver.new()
    #   factor = add(mtp(2,sin('x')),10)
    #   expect(solver.solvable_factor?(factor)).to eq true
    # end
  end

end
